import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: {
		// flexDirection: 'row',
		alignItems: 'center',
	},

	label: {
		fontWeight: 'bold',
		fontSize: 18,
		marginBottom: 10,
	},

	txt: {
		fontSize: 18,
	},
});

export { styles };
